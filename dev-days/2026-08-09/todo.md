# 2026-08-09 待办事项

- 继续验证账号与导出能力的端到端流程，重点看登录、我的记录、匿名记录绑定和 PDF 导出入口是否全部闭环。
- 完成 `Friends Tycoon` 的 6 人边界测试，重点检查房间人数上限、回合流转、退出与破产后的状态一致性。
- 如发现 `README.md`、`docs/` 与当前实现仍有偏差，优先把差异记录到当天文档，不要直接大改代码。
- 视后端最新状态决定是否需要重新运行最新版 `supabase/schema.sql` 再做一次线上回归。
