# 已完成

- 用户在 Supabase SQL Editor 运行 `supabase/schema.sql`，返回 `Success. No rows returned.`。
- 通过 Supabase RPC 探测确认 `qa_get_room` 函数已经可访问。
- 新增 `scripts/verify-online.mjs`，用于验证 GitHub Pages 线上网页和 Supabase 后端的完整答题流程。
- 完成线上双玩家自动化验收：测试房间 `0AE672` 中 2 位玩家均提交 100 题，结果页显示 100 个题目卡和 200 条答案。
- 验证提交后不可修改：结果页没有可编辑输入框，并显示答案锁定状态。
- 验证移动端首页首屏可正常显示创建房间入口和视觉图片。
- 更新 `README.md` 和 `docs/deployment-guide.md`，记录当前线上已可用状态和线上验收命令。
- 开始房间级题库改造：前端支持创建房间时选择默认题库或自定义题库。
- 自定义题库支持粘贴题目和上传 `.txt` / `.md` / `.csv` 纯文本文件。
- 答题页、进度、参与者状态、结果页均改为读取当前房间绑定的题库。
- 更新 `supabase/schema.sql`，为 `qa_rooms` 增加 `questions jsonb` 字段，并升级 `qa_create_room` 接收题库。
- 更新本地与线上验证脚本，覆盖自定义题库房间。
- 本地完整自动化验收通过：测试房间 `RF96CP` 使用上传的自定义题库，第 23 题为 `自定义问题 23?`，提交和结果页规则正常。
- 同步更新产品需求、技术架构、设计规范、开发计划、验收标准和上线指南。
- 已提交并推送房间级自定义题库改造，提交为 `678a85e Support room-specific question banks`。
- 确认 GitHub Pages 已能访问包含自定义题库代码的新版 `src/app.js`。
- 运行新版 Supabase SQL 后，确认 `qa_create_room(p_questions)` 已生效。
- 线上验收发现提交后仍有待发送的草稿同步请求，已在 `src/app.js` 中补充提交前清理和延迟请求二次检查，避免提交成功后产生 400 控制台错误。
- 线上自定义题库完整验收通过：测试房间 `25F8CE` 中 2 位玩家均提交 100 题，结果页显示 100 个题目卡、200 条答案，并确认自定义第 23 题可见。
- 验证移动端首页在新版线上环境中仍正常显示。
- 修复自定义题库粘贴体验：生成房间按钮不再静默禁用，题数不足时会提示已识别题数和还差几题。
- 改进题库解析：支持从一整段文本中识别 `1. ... 2. ...` 这类编号题目。
